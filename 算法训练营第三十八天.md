# 代码随想录算法训练营第三十八天|**739. 每日温度**，**496.下一个更大元素 I**，**503.下一个更大元素II** 

##  **739. 每日温度** 

[739. 每日温度 | 单调栈 | 递增栈 | 右边第一个更大元素 | 代码随想录](https://programmercarl.com/0739.每日温度.html#算法公开课)

## 我的思路

## 问题总结

## 卡的思路

学习笔记。

**通常是一维数组，要寻找任一个元素的右边或者左边第一个比自己大或者小的元素的位置，此时我们就要想到可以用单调栈了**。

**更直白来说，就是用一个栈来记录我们遍历过的元素**

## 我的代码

```
class Solution {
public:
    vector<int> dailyTemperatures(vector<int>& temperatures) {
        vector<int>result(temperatures.size(),0);
        stack<int>st;
        st.push(0);
        for(int i=1;i<temperatures.size();i++){
            if(temperatures[i]>temperatures[st.top()]){
                while(!st.empty()&&temperatures[i]>temperatures[st.top()]){
                    result[st.top()]=i-st.top();
                    st.pop();
                }
                st.push(i);
              
            }
              else
                st.push(i);
        }
        return result;
    }
};
```



## **496.下一个更大元素 I** 

[496.下一个更大元素 I | 单调栈 | 哈希表 | 代码随想录](https://programmercarl.com/0496.下一个更大元素I.html)

## 我的思路

## 问题总结

有段时间没写map和栈的操作了，感觉很不熟悉

## 卡的思路

## 我的代码

```
class Solution {
public:
    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {
        if (nums2.size() == 0 || nums1.size() == 0)
            return vector<int>(nums1.size(), -1);

        vector<int> result(nums1.size(), -1);
        stack<int> st;
        map<int, int> mp;

        for (int i = 0; i < nums1.size(); i++)
            mp[nums1[i]] = i;

        st.push(0);
        for (int i = 1; i < nums2.size(); i++) {
            while (!st.empty() && nums2[i] > nums2[st.top()]) {
                if (mp.count(nums2[st.top()]) > 0) { 
                    int index = mp[nums2[st.top()]];
                    result[index] = nums2[i];
                }
                st.pop();
            }
            st.push(i);
        }
        return result;
    }
};
```



## **503.下一个更大元素II** 

[503.下一个更大元素II | 单调栈 | 循环数组 | 成环搜索 | 代码随想录](https://programmercarl.com/0503.下一个更大元素II.html)

## 我的思路

## 问题总结

## 卡的思路

## 我的代码

```
class Solution {
public:
    vector<int> nextGreaterElements(vector<int>& nums) {
        vector<int>result(nums.size(),-1);
        stack<int>st;
        st.push(0);
        for(int i=0;i<nums.size()*2;i++){
            if(nums[i%nums.size()]>nums[st.top()]){
                while(!st.empty()&&nums[i%nums.size()]>nums[st.top()]){
                    result[st.top()]=nums[i%nums.size()];
                    st.pop();
                }
                st.push(i%nums.size());
            }
            else 
            st.push(i%nums.size());
        }
        return result;
    }
};
```

