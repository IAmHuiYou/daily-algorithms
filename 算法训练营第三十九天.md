# 代码随想录算法训练营第三十九天|42. 接雨水，84.**柱状图中最大的矩形**

## 42. 接雨水

[42. 接雨水 | 单调栈 | 双指针 | 凹槽计算 | 代码随想录](https://programmercarl.com/0042.接雨水.html#算法公开课)

## 我的思路

## 问题总结

## 卡的思路

其实就是对每个柱子计算右边比它高的第一个柱子、左边比她高的第一个柱子。

加入的数如果比栈顶大，这个数就是栈顶元素右边的第一个高柱子，栈顶元素的栈下面的第一个元素就是左边第一个。因为栈是递增栈。

单调栈：存放之前遍历过的元素。

遇到相同的元素，两种处理方式：

直接加入栈或弹出相同元素再加入新元素，结果一样的。

mid=st.top()

st.pop()

left=st.top()

h=min(left,right)-mid;

横向求解

## 我的代码

```
class Solution {
public:
    int trap(vector<int>& height) {
        if (height.size() <= 2) return 0;
        stack<int>st;
        st.push(0);
        int result=0;
        for(int i=1;i<height.size();i++){
            if(height[i]<height[st.top()])st.push(i);
            else if(height[i]==height[st.top()]){
                st.pop();
                st.push(i);
            }
            else{
                while(!st.empty()&&height[i]>height[st.top()]){
                    int mid=st.top();
                    st.pop();
                    if(!st.empty()){
                        int h=min(height[st.top()],height[i])-height[mid];
                        int w=i-st.top()-1;
                        result+=w*h;
                    }
                }
                st.push(i);
            }
        }
        return result;
    }
};
```



## 84.**柱状图中最大的矩形**

[84.柱状图中最大的矩形 | 单调栈 | 柱状图最大矩形面积 | 左右两侧第一个小于该柱子的柱子 | 代码随想录](https://programmercarl.com/0084.柱状图中最大的矩形.html)

## 我的思路



## 问题总结

## 卡的思路

本题是找每个柱子左右两边第一个小于该柱子的柱子。

只有栈里从大到小的顺序，才能保证栈顶元素找到左右两边第一个小于栈顶元素的柱子。

来说末尾为什么要加元素0？

如果数组本身就是升序的，例如[2,4,6,8]，那么入栈之后 都是单调递减，一直都没有走 情况三 计算结果的哪一步，所以最后输出的就是0了。 

## 卡的代码

```
class Solution {
public:
    int largestRectangleArea(vector<int>& heights) {
        int result = 0;
        stack<int> st;
        heights.insert(heights.begin(), 0); // 数组头部加入元素0
        heights.push_back(0); // 数组尾部加入元素0
        st.push(0);

        // 第一个元素已经入栈，从下标1开始
        for (int i = 1; i < heights.size(); i++) {
            if (heights[i] > heights[st.top()]) { // 情况一
                st.push(i);
            } else if (heights[i] == heights[st.top()]) { // 情况二
                st.pop(); // 这个可以加，可以不加，效果一样，思路不同
                st.push(i);
            } else { // 情况三
                while (!st.empty() && heights[i] < heights[st.top()]) { // 注意是while
                    int mid = st.top();
                    st.pop();
                    if (!st.empty()) {
                        int left = st.top();
                        int right = i;
                        int w = right - left - 1;
                        int h = heights[mid];
                        result = max(result, w * h);
                    }
                }
                st.push(i);
            }
        }
        return result;
    }
};
```

