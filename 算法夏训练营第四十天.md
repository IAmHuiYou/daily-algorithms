# 代码随想录算法训练营第四十天|**98. 所有可达路径**

图论基础

[图论理论基础 | 图论 | 有向图 | 无向图 | 代码随想录](https://www.programmercarl.com/kamacoder/图论理论基础.html)

[深度优先搜索理论基础 | 深度优先搜索 | 回溯 | 递归 | 代码随想录](https://www.programmercarl.com/kamacoder/图论深搜理论基础.html#dfs-与-bfs-区别)

## **98. 所有可达路径**

https://www.programmercarl.com/kamacoder/%E5%9B%BE%E8%AE%BA%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html

## 我的思路

## 问题总结

## 卡的思路

### 图的存储

**1.邻接矩阵**

 邻接矩阵是从节点的角度来表示图，有多少节点就申请多大的二维数组。

本题我们会有n 个节点，因为节点标号是从1开始的，为了节点标号和下标对齐，我们申请 n + 1 * n + 1 这么大的二维数组。

```cpp
vector<vector<int>> graph(n + 1, vector<int>(n + 1, 0));
```

输入m个边，构造方式如下：

```cpp
while (m--) {
    cin >> s >> t;
    // 使用邻接矩阵 ，1 表示 节点s 指向 节点t
    graph[s][t] = 1;
}
```

2.**邻接表**

我们需要构造一个数组，数组里的元素是一个链表。

```cpp
// 节点编号从1到n，所以申请 n+1 这么大的数组
vector<list<int>> graph(n + 1); // 邻接表，list为C++里的链表
```

```cpp
while (m--) {
    cin >> s >> t;
    // 使用邻接表 ，表示 s -> t 是相连的
    graph[s].push_back(t);
}
```

## 我的代码

```
#include<iostream>
#include<vector>
using namespace std;
vector<vector<int>>result;
vector<int>path;
void dfs(vector<vector<int>>&grapth,int n,int x){
    if(n==x){
        result.push_back(path);
        return;
    }
    for(int i=1;i<=n;i++){
        if(grapth[x][i]!=0){
            path.push_back(i);
            dfs(grapth,n,i);
            path.pop_back();
        }
    }
    return;
}

int main(){
    int n,m,s,t;
    cin>>n>>m;
    vector<vector<int>>grapth(n+1,vector<int>(n+1,0));
    for(int i=1;i<=m;i++){
        cin>>s>>t;
        grapth[s][t]=1;
    }

    path.push_back(1);
    dfs(grapth,n,1);

    if(result.size()==0)cout<<"-1"<<endl;
    for(const vector<int>&pa:result){
        for(int i=0;i<pa.size()-1;i++)
        cout<<pa[i]<<' ';
        cout<<pa[pa.size()-1]<<endl;;
    }
}

```

